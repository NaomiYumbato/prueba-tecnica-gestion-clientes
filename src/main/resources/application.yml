server:
  port: 8080

spring:
  application:
    name: prueba-tecnica-gestion-clientes
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/db_prueba_tecnica
    username: ${PSQL_DB_USER}
    password: ${PSQL_DB_PASSWORD}
